# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP.NET Core service.

version: '3.4'

services:
  githubstartrackerapi:
    image: githubstartrackerapi
    build:
      context: .
      dockerfile: GithubStarTracker.API/Dockerfile
    ports:
      - 5276:5276
